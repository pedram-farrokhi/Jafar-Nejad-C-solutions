//دستوراتی بنویسید که متغیر های ایکس و ایگرگ را از نوع عدد صحیح و کاراکتر و متغیر دی را از نوع دابل و ثابت کا را را از نوع عدد صحیح آنسایند با مقدار اولیه 20 تعریف کند 
//Write statements that define the variables x and y as integers and character, the variable d as double, and the constant k as an unsigned integer with an initial value of 20.

#include <iostream>
using namespace std;

int main()
{
  int x;
  char y;
  double d;
  const unsigned int k = 20;
  return 0;
}
