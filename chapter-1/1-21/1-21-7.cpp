//با توجه به مقادیر اعیین شده هر یک از عبارات زیر را ارزیابی کنید 
//Evaluate each of the following statements according to the values given:

int main() 
{
    //first
    int x = 8, y = 10, m = 6;
    int t = 0; 
    int k;
    
    //second
    k = x / y + t + y / -m;
    cout << k << endl;
    
    // third
    k = x / 4 * y / 2 * m;
    cout << k << endl;
    
    return 0;
}
